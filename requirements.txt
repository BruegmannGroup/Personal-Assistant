faster-whisper>=0.8.0
# LLM providers — only the one selected via LLM_PROVIDER needs a valid API key,
# but all three packages are installed so switching providers needs no reinstall.
google-genai>=0.3.0
openai>=1.0.0
anthropic>=0.30.0
python-dotenv>=1.0.0
jsonschema>=4.0.0
ffmpeg-python>=0.2.0
pydub>=0.25.1
requests>=2.28.0
streamlit>=1.30.0
